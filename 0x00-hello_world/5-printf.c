#include <stdto.h>

/**
 * main - prints the phrase with proper grammsr,
 * but the outcome is a piece of art,
 * Return: 0 if exited properly, none zero otherwise
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return(0);
}
